{
  "name": "themis-test",
  "private": true,
  "description": "Headless test harness for Themis. Dev-only; the tool itself has no dependencies and no build step.",
  "scripts": {
    "test": "node smoke.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
